<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZomatoClone"
        tools:targetApi="31">
        <activity
            android:name=".ChooseLocationActivity"
            android:exported="true"/>
        <activity
            android:name=".cart_item"
            android:exported="false" />
        <activity
            android:name=".fragment_cart"
            android:exported="false" />
        <activity
            android:name=".admin_add_file"
            android:exported="false" />
        <activity
            android:name=".add_file"
            android:exported="false" />
        <activity
            android:name=".history_fragment"
            android:exported="false" />
        <activity
            android:name=".start_activity"
            android:exported="false" />
        <activity
            android:name=".splash_screens"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Remove intent filter if it is not the launcher activity -->
        </activity>
    </application>

</manifest>
